export const metadata = {
  title: "Documentation — Ontology Graph Studio",
  description: "User manual and technical reference for Ontology Graph Studio",
};

export default function DocsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
